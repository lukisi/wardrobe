# ========================================================
# .env.example - Template per le variabili d'ambiente
# ========================================================
# Questo file è un esempio: COPIALO e rinominalo in base all'ambiente!
# NON committare MAI i file .env veri su git (sono già ignorati in .gitignore)
#
# Regole generali:
# - Solo variabili che iniziano con VITE_ sono disponibili nel codice frontend
# - Le variabili vengono sostituite a build-time (non runtime)
# - Usa process.env in vite.config.ts, import.meta.env nel codice src/
#
# ========================================================
# File disponibili e quando vengono caricati:
#
# .env                  → Sempre (base per tutti gli ambienti)
# .env.development      → Solo durante yarn dev (sviluppo locale)
# .env.production       → Solo durante yarn build (produzione/deploy)
# .env.local            → Sempre, con priorità massima (override personale)
#
# ========================================================
# Esempi di variabili comuni per questo progetto

# URL base del backend Django (senza /api/ finale)
VITE_API_URL=http://127.0.0.1:8000

# (opzionale) Per debug o feature toggle in sviluppo
# VITE_DEBUG=true

# (opzionale) URL produzione dopo deploy
# VITE_API_URL=https://mia-app.com/api

# NON mettere qui segreti veri (API keys, password, token)!
# ========================================================