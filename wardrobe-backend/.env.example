# ========================================================
# .env.example - Template per le variabili d'ambiente
# ========================================================
# Questo file è un ESEMPIO: COPIALO e rinominalo in .env
# NON committare MAI il file .env su git (già ignorato in .gitignore)
#
# Regole generali:
# - Usa python-dotenv per caricare automaticamente .env in settings.py
# - Le variabili vengono lette con os.getenv() o environs
# - In produzione (Render, Railway, ecc.) le imposti come Environment Variables
#   nel pannello del provider (non serve file .env fisico)
#
# ========================================================
# Variabili consigliate per questo progetto

# SECRET_KEY=la_tua_chiave_segreta_molto_lunga_e_random  # generane una nuova con: python -c "import secrets; print(secrets.token_urlsafe(50))"
DEBUG=True

# Connessione database (Postgres in produzione)
# In sviluppo locale: se non presente, usa SQLite con BASE_DIR/db.sqlite3
# DATABASE_URL=postgres://utente:password@host:5432/nome_db?sslmode=require

# NON mettere qui dati sensibili veri se il file viene committato!
# ========================================================